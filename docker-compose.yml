services:
  neogenealogy:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
      - ./test-data:/app/test-data:ro
    environment:
      - NEOGENEALOGY_HOST=0.0.0.0
      - NEOGENEALOGY_PORT=3000
      - NEOGENEALOGY_CORS_ORIGIN=*
      - RUST_LOG=info
